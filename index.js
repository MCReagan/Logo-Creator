// this imports the required js file to run promptUser()
const { promptUser, setShape, makeLogo } = require('./lib/logo.js')

// this starts promptUser on logo.js when node runs index.js
promptUser();

